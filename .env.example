# Copia a .env solo para «netlify dev» local (NO subir .env al repo).
# En producción usa Environment variables del sitio Netlify (ya las tienes).

# Mismo valor que Apps Script → Propiedades del script → API_TOKEN
API_TOKEN=

# URL …/exec del Web App de permisos (BD-PERMISOS)
PERMISOS_SCRIPT_URL=
